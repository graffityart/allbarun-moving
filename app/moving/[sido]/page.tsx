import type { Metadata } from "next";import { notFound } from "next/navigation";import SiteHeader from "@/components/SiteHeader";import SiteFooter from "@/components/SiteFooter";import { regionProfiles } from "@/lib/regions";import{ESTIMATE_INQUIRY_URL}from"@/lib/external-links";
type Props={params:Promise<{sido:string}>};
export async function generateStaticParams(){return regionProfiles.map(r=>({sido:r.slug}))}
export async function generateMetadata({params}:Props):Promise<Metadata>{const{sido}=await params;const region=regionProfiles.find(r=>r.slug===sido);if(!region)return{};return{title:`${region.name} 이사 지역 가이드 | 시·군·구별 이사업체 비교`,description:`${region.name}의 ${region.districts.length}개 시·군·구별 이사 조건과 지역 특징을 확인하고 포장이사·원룸이사·사무실이사 견적을 비교하세요.`,alternates:{canonical:`/moving/${region.slug}`}}}
export default async function ProvinceHub({params}:Props){const{sido}=await params;const region=regionProfiles.find(r=>r.slug===sido);if(!region)notFound();const base=(process.env.NEXT_PUBLIC_SITE_URL||"https://example.com").replace(/\/$/,"");const pageUrl=`${base}/moving/${region.slug}`;const breadcrumb={"@context":"https://schema.org","@type":"BreadcrumbList",itemListElement:[{"@type":"ListItem",position:1,name:"홈",item:base},{"@type":"ListItem",position:2,name:"전국 지역별 이사",item:`${base}/moving`},{"@type":"ListItem",position:3,name:`${region.name} 이사`,item:pageUrl}]};return <><SiteHeader/><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(breadcrumb)}}/><main><section className="sub-hero"><div className="wrap narrow"><div className="breadcrumb"><a href="/">홈</a><span>›</span><a href="/moving">지역별 이사</a><span>›</span><strong>{region.name}</strong></div><span className="eyebrow">{region.name} 이사 지역 가이드</span><h1>{region.name} 안에서도<br/>이사 조건은 지역마다 다릅니다.</h1><p>{region.summary}</p></div></section><section className="section white"><div className="wrap"><div className="tips-grid"><article className="card"><strong>주거 형태</strong><p>{region.housing}</p></article><article className="card"><strong>차량 접근</strong><p>{region.access}</p></article><article className="card"><strong>이동 시간</strong><p>{region.traffic}</p></article></div></div></section><section className="section"><div className="wrap"><span className="eyebrow">시·군·구 선택</span><h2 className="section-title">{region.name} 지역별 이사 정보</h2><p className="section-desc">출발지 또는 도착지를 선택하면 해당 지역의 생활권과 차량 접근, 손없는날·날씨, 비교견적 체크포인트를 확인할 수 있습니다.</p><div className="province-district-grid">{region.districts.map(d=><a key={d} href={`/moving/${region.slug}/${encodeURIComponent(d)}`} className="province-district-card"><strong>{d}</strong><span>{d} 이사 정보 보기 →</span></a>)}</div></div></section><section className="section white"><div className="wrap two-col-info"><div><span className="eyebrow">견적 전에</span><h2 className="section-title">{region.name} 이사에서 공통으로 확인할 것</h2><ul className="check-list"><li>출발지·도착지의 층수와 엘리베이터 사용 가능 여부</li><li>이사차량 정차 위치와 현관까지 실제 운반거리</li><li>관리사무소의 이사시간·사다리차·보양 관련 규정</li><li>주말·월말·손없는날 등 선호 날짜의 예약 여유</li><li>대형가전·가구 분해조립과 추가비 기준</li></ul></div><div className="highlight-card"><strong>{region.name} 이사 팁</strong><p>{region.tip}</p><a href={ESTIMATE_INQUIRY_URL}>무료 비교견적 시작 →</a></div></div></section></main><SiteFooter/></>}
