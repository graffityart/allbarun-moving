import type { Metadata } from "next";
export const metadata:Metadata={title:"관리자 설정 | 올바른이사",robots:{index:false,follow:false}};
export default function AdminSetup(){const configured=Boolean(process.env.SUPABASE_URL&&process.env.SUPABASE_SERVICE_ROLE_KEY);return <main className="admin-setup"><div className="admin-shell"><span className="eyebrow">운영자 전용</span><h1>올바른이사 관리자</h1><p className="admin-lead">견적 접수 DB와 관리자 인증을 연결하면 이 화면에서 신규 접수와 상담 상태를 관리할 수 있습니다.</p><div className="admin-status"><span className={configured?"status-dot ready":"status-dot"}/><div><strong>{configured?"DB 환경변수 연결됨":"DB 연결 대기중"}</strong><p>{configured?"다음 단계에서 인증된 견적 목록을 활성화할 수 있습니다.":"Supabase 프로젝트와 Vercel 서버 환경변수를 설정하면 접수 저장 기능을 활성화할 수 있습니다."}</p></div></div><div className="admin-grid"><article><strong>신규 접수</strong><p>접수번호, 이사일, 출발·도착지역을 기준으로 확인하는 구조를 준비합니다.</p></article><article><strong>상담 상태</strong><p>신규 → 상담중 → 견적완료 → 완료/취소 순서로 관리합니다.</p></article><article><strong>개인정보 보호</strong><p>관리자 인증 전에는 고객 이름·전화번호 등 접수 데이터가 브라우저에 노출되지 않습니다.</p></article></div><div className="admin-note"><strong>현재 단계</strong><p>관리자 인증이 없는 공개 목록 페이지는 만들지 않았습니다. 실제 운영 전 인증과 DB 권한을 먼저 연결합니다.</p></div><a className="admin-home" href="/">사이트로 돌아가기</a></div></main>}
