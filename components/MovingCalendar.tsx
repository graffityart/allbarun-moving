"use client";
import { useEffect,useMemo,useState } from "react";import KoreanLunarCalendar from "korean-lunar-calendar";import{ESTIMATE_INQUIRY_URL}from"@/lib/external-links";
const LOCATIONS=[{name:"서울",aliases:["서울특별시"],lat:37.5665,lon:126.978},{name:"부산",aliases:["부산광역시"],lat:35.1796,lon:129.0756},{name:"대구",aliases:["대구광역시"],lat:35.8714,lon:128.6014},{name:"인천",aliases:["인천광역시"],lat:37.4563,lon:126.7052},{name:"광주",aliases:["광주광역시"],lat:35.1595,lon:126.8526},{name:"대전",aliases:["대전광역시"],lat:36.3504,lon:127.3845},{name:"울산",aliases:["울산광역시"],lat:35.5384,lon:129.3114},{name:"세종",aliases:["세종특별자치시"],lat:36.4801,lon:127.289},{name:"경기",aliases:["경기도","수원"],lat:37.2636,lon:127.0286},{name:"강원",aliases:["강원도","강원특별자치도","춘천"],lat:37.8813,lon:127.7298},{name:"충북",aliases:["충청북도","청주"],lat:36.6424,lon:127.489},{name:"충남",aliases:["충청남도","홍성"],lat:36.6012,lon:126.6608},{name:"전북",aliases:["전라북도","전북특별자치도","전주"],lat:35.8242,lon:127.148},{name:"전남",aliases:["전라남도","무안"],lat:34.9905,lon:126.4817},{name:"경북",aliases:["경상북도","안동"],lat:36.5684,lon:128.7294},{name:"경남",aliases:["경상남도","창원"],lat:35.2279,lon:128.6811},{name:"제주",aliases:["제주도","제주특별자치도"],lat:33.4996,lon:126.5312}] as const;
type WeatherDay={date:string;min:number;max:number;rain:number;code:number};type Props={regionName?:string;districtName?:string;lockRegion?:boolean};
function weatherText(code:number){if(code===0)return"맑음";if([1,2].includes(code))return"대체로 맑음";if(code===3)return"흐림";if([45,48].includes(code))return"안개";if([51,53,55,56,57].includes(code))return"이슬비";if([61,63,65,66,67,80,81,82].includes(code))return"비";if([71,73,75,77,85,86].includes(code))return"눈";if([95,96,99].includes(code))return"뇌우";return"변동 가능"}
function isHandsFreeDay(date:Date){const calendar=new KoreanLunarCalendar();const ok=calendar.setSolarDate(date.getFullYear(),date.getMonth()+1,date.getDate());if(!ok)return false;const lunar=calendar.getLunarCalendar();return lunar.day%10===9||lunar.day%10===0}
function regionFallback(regionName?:string){const value=(regionName||"").trim();return LOCATIONS.find(item=>item.name===value||item.aliases.some(alias=>alias===value))||LOCATIONS.find(item=>value.startsWith(item.name)||item.aliases.some(alias=>value.startsWith(alias)))||LOCATIONS[0]}
function dateKey(date:Date){const y=date.getFullYear();const m=String(date.getMonth()+1).padStart(2,"0");const d=String(date.getDate()).padStart(2,"0");return `${y}-${m}-${d}`}
export default function MovingCalendar({regionName,districtName,lockRegion=false}:Props){const today=useMemo(()=>new Date(),[]);const[monthOffset,setMonthOffset]=useState(0);const[location,setLocation]=useState(LOCATIONS[0]);const[weather,setWeather]=useState<WeatherDay[]>([]);const[loading,setLoading]=useState(true);const displayArea=districtName?`${regionName??""} ${districtName}`.trim():location.name;const viewDate=useMemo(()=>new Date(today.getFullYear(),today.getMonth()+monthOffset,1),[today,monthOffset]);const days=useMemo(()=>{const year=viewDate.getFullYear(),month=viewDate.getMonth(),firstDay=new Date(year,month,1).getDay(),lastDate=new Date(year,month+1,0).getDate();const cells:Array<Date|null>=Array.from({length:firstDay},()=>null);for(let day=1;day<=lastDate;day++)cells.push(new Date(year,month,day));while(cells.length%7!==0)cells.push(null);return cells},[viewDate]);
useEffect(()=>{const controller=new AbortController();async function resolveLocation(){if(!regionName&&!districtName)return location;const fallback=regionFallback(regionName);const queries=[`${districtName??""} ${regionName??""} 대한민국`.trim(),`${districtName??""} 대한민국`.trim(),`${regionName??""} 대한민국`.trim()].filter(Boolean);for(const text of queries){try{const res=await fetch(`https://geocoding-api.open-meteo.com/v1/search?name=${encodeURIComponent(text)}&count=3&language=ko&format=json`,{signal:controller.signal});if(!res.ok)continue;const data=await res.json();const hit=data?.results?.find((x:any)=>x.country_code==="KR")??data?.results?.[0];if(hit)return{name:displayArea,lat:hit.latitude,lon:hit.longitude}}catch(error){if((error as Error).name==="AbortError")throw error}}return{name:displayArea||fallback.name,lat:fallback.lat,lon:fallback.lon}}
async function loadWeather(){setLoading(true);try{const resolved=await resolveLocation();if(regionName||districtName)setLocation(resolved as any);const url=new URL("https://api.open-meteo.com/v1/forecast");url.searchParams.set("latitude",String(resolved.lat));url.searchParams.set("longitude",String(resolved.lon));url.searchParams.set("daily","weather_code,temperature_2m_max,temperature_2m_min,precipitation_probability_max");url.searchParams.set("timezone","Asia/Seoul");url.searchParams.set("forecast_days","16");const res=await fetch(url.toString(),{signal:controller.signal});if(!res.ok)throw new Error("weather request failed");const data=await res.json();const next:WeatherDay[]=data.daily.time.map((date:string,i:number)=>({date,min:Math.round(data.daily.temperature_2m_min[i]),max:Math.round(data.daily.temperature_2m_max[i]),rain:data.daily.precipitation_probability_max[i]??0,code:data.daily.weather_code[i]}));setWeather(next)}catch(error){if((error as Error).name!=="AbortError")setWeather([])}finally{setLoading(false)}}loadWeather();return()=>controller.abort()},[regionName,districtName,displayArea]);const goodDays=days.filter((d):d is Date=>Boolean(d&&isHandsFreeDay(d)));const hasFutureGoodDay=goodDays.some(d=>dateKey(d)>=dateKey(today));
return <div className="calendar-shell trusted-calendar"><div className="calendar-card"><div className="calendar-head"><div><div className="small">이사 날짜 고르기</div><h3>{viewDate.getFullYear()}년 {viewDate.getMonth()+1}월 손없는날</h3></div><div className="calendar-nav"><button className="calendar-nav-btn" onClick={()=>setMonthOffset(v=>v-1)} aria-label="이전 달">‹</button><button className="calendar-nav-btn" onClick={()=>setMonthOffset(v=>v+1)} aria-label="다음 달">›</button></div></div><div className="calendar-grid">{["일","월","화","수","목","금","토"].map(d=><div className="dow" key={d}>{d}</div>)}{days.map((date,index)=>{if(!date)return <div className="day muted" key={`empty-${index}`}/>;const good=isHandsFreeDay(date),isToday=date.toDateString()===today.toDateString();return <div className={`day ${good?"good":""} ${isToday?"today":""}`} key={date.toISOString()}><div className="day-number">{date.getDate()}</div>{good&&<span className="good-label">손없는날</span>}</div>})}</div><div className="legend"><span><i className="dot"/>손없는날 {goodDays.length}일</span><span>※ 전통적으로 음력 날짜 끝자리가 9·0인 날을 표시합니다.</span></div>{hasFutureGoodDay&&<div className="good-day-actions"><strong>손없는날 이사 견적이 필요하신가요?</strong><div><a href={ESTIMATE_INQUIRY_URL}>비교견적 문의하기 →</a></div><p>견적 문의는 외부 비교견적 신청 페이지에서 진행됩니다. 날짜와 출발·도착지역을 해당 페이지에서 입력해주세요.</p></div>}</div><div className="weather-card"><div className="weather-top"><div><div className="small">이사 날씨 미리보기</div><strong className="weather-title">{displayArea} 16일 예보</strong></div>{!lockRegion&&!districtName&&<select className="field weather-select" value={location.name} onChange={e=>setLocation((LOCATIONS.find(l=>l.name===e.target.value)||LOCATIONS[0]) as any)}>{LOCATIONS.map(item=><option key={item.name}>{item.name}</option>)}</select>}</div><div className="weather-list">{loading&&<div className="weather-meta">해당 지역 날씨 정보를 불러오는 중입니다.</div>}{!loading&&weather.length===0&&<div className="weather-meta">현재 날씨 정보를 불러올 수 없습니다.</div>}{weather.slice(0,8).map(item=>{const d=new Date(`${item.date}T00:00:00`);return <div className="weather-row" key={item.date}><div><div className="weather-date">{d.getMonth()+1}/{d.getDate()} ({["일","월","화","수","목","금","토"][d.getDay()]})</div><div className="weather-meta">{weatherText(item.code)}</div></div><strong>{item.min}° / {item.max}°</strong><span className="weather-meta">강수 {item.rain}%</span></div>})}</div><div className="weather-note">{displayArea} 기준 단기 예보입니다. 뒤쪽 날짜일수록 예보 변동성이 커질 수 있으므로 이사일 2~3일 전 최신 예보를 다시 확인하세요. 비·눈·강풍 예보가 있으면 포장재와 차량 접근 동선도 함께 점검하는 것이 좋습니다.</div></div></div>}
